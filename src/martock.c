#include <martock.h>
#include <time.h>
 
#define C 10000

int main (int argc, char **argv)
{
        init();

        world_view();

        /* chunk *ch = chunk_generate(CHUNK_FULL, NULL, 0);
        chunk_save_img(ch);
        chunk_close(ch); */

        return 0;
}
